// Importar la configuración centralizada
import { API_CONFIG } from "./api-config";

// Exportar la URL de la API para mantener la compatibilidad con el código existente
export const API_URL = API_CONFIG.API_URL;
